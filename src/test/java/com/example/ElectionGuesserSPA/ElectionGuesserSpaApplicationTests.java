package com.example.ElectionGuesserSPA;
/*
import static org.junit.Assert.assertEquals;
import static org.mockito.Mockito.when;

import org.junit.jupiter.api.Test;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;

import org.mockito.junit.MockitoJUnitRunner;
import org.springframework.boot.test.context.SpringBootTest;

import com.kevin.sp.ElectionGuesserSPA.dao.CountyDAO;
import com.kevin.sp.ElectionGuesserSPA.entity.County;
import com.kevin.sp.ElectionGuesserSPA.service.CountyServiceImpl;

/*
 Sample test of the DAO
	1. Creates a mock DAO
	2. Injects the service into the DAO
	3. Create an object
	4. Check that object created matches object in DAO
*/
/*
@SpringBootTest
@RunWith(MockitoJUnitRunner.class) */
class ElectionGuesserApplicationTests {
/*
	@Mock
	private CountyDAO dao;
	
	@InjectMocks
	private CountyServiceImpl cs;
	
	@Test
	public void printCounty()
	{
		County county = new County();
		county.setFips(1001);
		when(dao.findById(1)).thenReturn(county);
		
		County county2 = cs.findById(1);
		
		assertEquals(county.getFips(),county2.getFips());
	}
	*/
}
